import { renderComponent, expect } from "../test_helper";
import CommentBox from "../../src/components/comment_box";

describe('CommentBox', () => {
  it("Has a text area", () => {
  });

  it("Has a button", () => {
  });
});
